<header class="mb-5">
    <h1 class="text-2xl font-semibold text-slate-800">Tarifs</h1>
    <p class="text-sm text-slate-500">Gérer les différents tarifs et taxes.</p>
</header>

<section>
    <div class="grid grid-cols-1 gap-3">
    <label class="text-sm grid gap-1">Prix / nuit (€)
        <input name="price" id="cfg_price_night" type="number" min="0" step="1" class="rounded-lg border px-3 py-2" placeholder="120" value="<%= config.price %>"/>
    </label>
    <label class="text-sm grid gap-1">Frais de ménage (€)
        <input name="cleaning_fees" id="cfg_cleaning_fees" type="number" min="0" step="1" class="rounded-lg border px-3 py-2" placeholder="35" value="<%= config.cleaning_fees %>"/>
    </label>
    <label class="text-sm grid gap-1">Taxe (%)
        <input name="tax_rate" id="cfg_tax_rate" type="number" min="0" step="0.1" class="rounded-lg border px-3 py-2" placeholder="10" value="<%= config.tax_rate %>"/>
    </label>
    <label class="text-sm grid gap-1">Max voyageurs
        <input name="max_guests" id="cfg_max_guests" type="number" min="0" max="10" step="1" class="rounded-lg border px-3 py-2" placeholder="4" value="<%= config.max_guests %>"/>
    </label>
    </div>
</section>