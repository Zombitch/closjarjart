<div class="space-y-5">
    <div>
        <h3 class="font-semibold mb-2">Titre du logement</h3>
        <input type="text" name="title" id="cfg_title" value="<%= config.title %>" class="w-full rounded-lg border px-3 py-2" placeholder="Loft cocooning avec Sauna — 45m², 1 ch, 1 sde" />
    </div>
    <div>
        <h3 class="font-semibold mb-2">Description</h3>
        <textarea name="description" id="cfg_description" rows="5" class="w-full rounded-lg border px-3 py-2" placeholder="Décrivez l'espace, la disposition, les points forts, etc."><%= config.description %></textarea>
    </div>
    <div>
        <h3 class="font-semibold mb-2">Règles du logement</h3>
        <textarea name="rules" id="cfg_rules" rows="5" class="w-full rounded-lg border px-3 py-2" placeholder="Ex. non-fumeur, pas de fêtes, horaires de calme, etc."><%= config.rules %></textarea>
    </div>
</div>